# Environment Variables Template
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to git

# ============================================
# Firebase Configuration
# Get these from: Firebase Console > Project Settings > General
# ============================================
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# ============================================
# Gemini AI Configuration
# Production: Gemini calls go through the geminiProxy Cloud Function.
#   Set VITE_CLOUD_FUNCTIONS_URL below. The API key is stored as a
#   Cloud Secret (GEMINI_API_KEY) and never reaches the client.
# Development only: Set VITE_GEMINI_API_KEY for local dev without
#   Cloud Functions deployed. Remove before production use.
# ============================================
# VITE_GEMINI_API_KEY=your_gemini_api_key_here  # DEV ONLY — remove for production

# ============================================
# Cloud Functions Configuration
# Provides link preview proxy, image proxy, and Gemini AI proxy.
# Get this from: Firebase Console > Functions > Dashboard
# ============================================
VITE_CLOUD_FUNCTIONS_URL=https://us-central1-your_project_id.cloudfunctions.net

# ============================================
# Observability (optional — app works without these)
# ============================================
VITE_SENTRY_DSN=
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://app.posthog.com
# Environment label shown in Sentry (defaults to Vite mode)
VITE_APP_ENV=development

# ============================================
# Development Feature Gates
# DEV ONLY: Bypass subscription gates for testing gated features
# Set to "true" to grant Pro access in development mode
# NEVER enable in production
# ============================================
# VITE_DEV_BYPASS_SUBSCRIPTION=true

# ============================================
# Google Calendar OAuth (Server-Side)
# The Client ID is used by the GIS SDK on the frontend to request
# an authorization code. The Client Secret is stored as a Firebase
# Cloud Secret (GOOGLE_CLIENT_SECRET) and NEVER reaches the client.
# ============================================
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here

# ============================================
# Stripe Payments (Phase 2)
# Publishable key is safe for client — used by Checkout redirect.
# Secret key and webhook secret are stored in GCP Secret Manager
# (STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET) — never here.
# ============================================
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# ============================================
# Razorpay Payments (Phase 2 — India provider)
# Key ID returned from createRazorpayOrder Cloud Function.
# Key Secret and Webhook Secret stored in GCP Secret Manager
# (RAZORPAY_KEY_SECRET, RAZORPAY_WEBHOOK_SECRET) — never here.
# ============================================

# ============================================
# Cloudflare Turnstile CAPTCHA
# Site key is public (embedded in client). Secret key stored in
# GCP Secret Manager as TURNSTILE_SECRET.
# If not set, CAPTCHA is skipped (dev mode).
# ============================================
VITE_TURNSTILE_SITE_KEY=your_turnstile_site_key_here
