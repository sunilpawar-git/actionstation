# Key Lifecycle Management

> **Version**: 1.0 | **Date**: 29 March 2026
> **Owner**: DevOps
> **Review cadence**: Quarterly

---

## Secrets Inventory

| Secret | Purpose | Storage | Access | Rotation Schedule |
|--------|---------|---------|--------|-------------------|
| `STRIPE_SECRET_KEY` | Server-side Stripe API calls | GCP Secret Manager | Cloud Functions SA | 90 days |
| `STRIPE_WEBHOOK_SECRET` | Webhook signature verification | GCP Secret Manager | Cloud Functions SA | 90 days |
| `GEMINI_API_KEY` | Gemini AI proxy | GCP Secret Manager | Cloud Functions SA | 90 days |
| `TURNSTILE_SECRET` | Turnstile CAPTCHA verification | GCP Secret Manager | Cloud Functions SA | 180 days |
| `RECAPTCHA_SECRET` | reCAPTCHA v3 verification | GCP Secret Manager | Cloud Functions SA | 180 days |
| `GOOGLE_CLIENT_SECRET` | Calendar OAuth | GCP Secret Manager | Cloud Functions SA | 365 days (Google-managed) |
| `URL_SIGNING_SECRET` | Image/link proxy URL signing | GCP Secret Manager | Cloud Functions SA | 90 days |

**Not in Secret Manager** (build-time env vars via GitHub Secrets):
- `VITE_STRIPE_PUBLISHABLE_KEY` — Safe for client (cannot access sensitive operations)
- `VITE_FIREBASE_*` — Firebase config (safe for client)
- `VITE_TURNSTILE_SITE_KEY` — Public site key (safe for client)

---

## Storage

- **All secrets stored exclusively in Google Cloud Secret Manager**
- Envelope encryption: secret encrypted with DEK → DEK encrypted with KEK in Cloud KMS
- Access requires both IAM permission AND KEK in Cloud KMS
- No secrets in `.env` files, GitHub Secrets (except `VITE_` prefixed), or code
- Cloud Audit Logs record every secret access (400-day retention)

---

## Generation

- All secrets generated by their respective providers (Stripe Dashboard, Cloudflare Dashboard, Google Cloud Console)
- No secrets generated manually or stored in plaintext
- Stripe keys generated with restricted permissions (read-only where possible)

---

## Rotation Procedure

### Standard Rotation (90-day cycle)

```
1. Generate new secret in provider dashboard
2. Add new version to Secret Manager:
   echo -n "NEW_SECRET_VALUE" | gcloud secrets versions add SECRET_NAME --data-file=-
3. Verify new version is accessible:
   gcloud secrets versions access latest --secret=SECRET_NAME
4. Cloud Functions automatically pick up new version on next cold start
5. Force cold start if immediate rotation needed:
   gcloud functions deploy FUNCTION_NAME --no-traffic (then restore)
6. Verify API calls succeed with new secret (monitor Cloud Logging)
7. Disable old secret version after 24-hour soak:
   gcloud secrets versions disable OLD_VERSION --secret=SECRET_NAME
8. Destroy old version after 30-day retention:
   gcloud secrets versions destroy OLD_VERSION --secret=SECRET_NAME
```

### Emergency Rotation (compromised key)

```
1. IMMEDIATE: Disable compromised version:
   gcloud secrets versions disable VERSION --secret=SECRET_NAME
2. Generate new key in provider dashboard
3. Add new version to Secret Manager
4. Force redeploy of affected Cloud Functions:
   firebase deploy --only functions:FUNCTION_NAME
5. Log security incident via logSecurityEvent('KEY_REVOCATION', {...})
6. Notify stakeholders per incident response procedure
7. Destroy compromised version after 30-day confirmation
```

---

## Rotation Schedule

| Secret | Next Rotation | Interval | Responsible |
|--------|--------------|----------|-------------|
| `STRIPE_SECRET_KEY` | Launch + 90d | 90 days | DevOps |
| `STRIPE_WEBHOOK_SECRET` | Launch + 90d | 90 days | DevOps |
| `GEMINI_API_KEY` | Existing schedule | 90 days | DevOps |
| `URL_SIGNING_SECRET` | Existing schedule | 90 days | DevOps |
| `TURNSTILE_SECRET` | Launch + 180d | 180 days | DevOps |
| `RECAPTCHA_SECRET` | Launch + 180d | 180 days | DevOps |
| `GOOGLE_CLIENT_SECRET` | Google-managed | 365 days | Google |

---

## Backup

- Secret Manager automatically retains all versions until explicitly destroyed
- Cross-region replication: Secret Manager replicates to multiple GCP regions by default
- Recovery: `gcloud secrets versions access VERSION_NUMBER --secret=SECRET_NAME`
- **No manual backup files** — Secret Manager IS the backup system

---

## Revocation

| Scenario | Action | Timeline |
|----------|--------|----------|
| Secret in committed code | Roll key in provider dashboard + update Secret Manager | <15 min |
| Secret in Cloud Logging | Rotate immediately + review log retention | <30 min |
| Secret in Sentry | Rotate immediately + verify Sentry data scrubbing | <30 min |
| Anomalous access pattern | Investigate + rotate if confirmed | <4 hours |

---

## Access Review

| Review | Frequency | Scope |
|--------|-----------|-------|
| Secret Manager IAM | Monthly | Who has `secretmanager.secretAccessor` |
| GCP IAM | Monthly | All project-level bindings |
| Firebase Console access | Monthly | Who has project access |
| Stripe Dashboard access | Monthly | Who has account access |

---

*Last reviewed: 29 March 2026*
*Next review: Launch + 90 days*
